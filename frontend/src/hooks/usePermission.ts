import {computed}from'vue';import {authStore}from'../stores/authStore';import type{PermissionCode}from'../types/permission';const map:Record<string,PermissionCode[]>={resident:[],staff:['repair:manage','payment:manage','announcement:publish','visitor:review','visitor:gate'],admin:['repair:manage','payment:manage','announcement:publish','log:read','visitor:review','visitor:gate']};export function usePermission(code:PermissionCode){return computed(()=>map[authStore.user?.role||'resident']?.includes(code)||false)}
